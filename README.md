# Hackathon-Starship-MedScan

Link do Video prezentacyjnego:
https://youtu.be/oUuAULoZ85c?is=nJr6-7I7VP5kCl2c
Film jest spóżniony z powodów technicznych.

ze wzgledu na duzy rozmiar modelu llm nie moglismy go zamiesic na githubie z powodu limitu 2GB

## 🛠️ Wymagania

* **JDK** (zalecana wersja 17 lub nowsza)
* **Gradle** (jeśli nie korzystasz z wrappera gradlew)
* **Aktywne połączenie z internetem**

## ⚙️ Instalacja i Konfiguracja

Aby uruchomić aplikację lokalnie, wykonaj poniższe kroki:

### 1. Pobierz klucz API Gemini
* Zaloguj się do **Google AI Studio**.
* Wygeneruj nowy klucz API (**API Key**).

### 2. Konfiguracja zmiennych środowiskowych
W głównym folderze projektu znajdziesz plik o nazwie `.env` wklej do niego swój klucz:

## 🚀 Uruchamianie

Projekt jest zarządzany przez system Gradle. Aby zbudować i uruchomić aplikację, otwórz terminal w folderze projektu i wpisz:

### System Windows:
```bash
./gradlew build
./gradlew run
```

### System Linux / macOS:
```bash
chmod +x gradlew
./gradlew build
./gradlew run
```
